import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
import test from "node:test";
const root=new URL("../",import.meta.url);
test("Pixel Flow exposes a local Bridge and structured Codex commands",async()=>{const[bridge,mcp,client,manifest,config]=await Promise.all([readFile(new URL("tools/pixel-flow-bridge/daemon.mjs",root),"utf8"),readFile(new URL("tools/pixel-flow-mcp/server.mjs",root),"utf8"),readFile(new URL("src/codex-bridge.tsx",root),"utf8"),readFile(new URL("public/manifest.json",root),"utf8"),readFile(new URL("package.json",root),"utf8")]);assert.match(bridge,/127\.0\.0\.1/);assert.match(bridge,/requestId and action are required/);assert.match(mcp,/pixel_flow_get_state/);assert.match(mcp,/pixel_flow_get_products/);assert.match(mcp,/pixel_flow_get_library/);assert.match(mcp,/pixel_flow_import_library/);assert.match(mcp,/does not perform AI classification/);assert.match(mcp,/pixel_flow_auto_tag_products/);assert.match(mcp,/pixel_flow_create_canvas/);assert.match(mcp,/pixel_flow_create_task/);assert.match(mcp,/pixel_flow_run_task/);assert.match(mcp,/pixel_flow_download_image/);assert.match(client,/canvas\.create/);assert.match(client,/task\.create/);assert.match(client,/task\.run/);assert.match(client,/history\.undo/);assert.match(client,/getProducts/);assert.match(client,/getLibrary/);assert.match(client,/library\.import/);assert.match(client,/library\.autoTagProducts/);assert.match(client,/imageDataUrl must be a base64 data URL/);assert.match(client,/包装盒/);assert.match(client,/铝箔/);assert.match(manifest,/43128/);assert.match(config,/bridge:install/)});

test("Pixel Flow ships a complete large-letter violet PF icon set",async()=>{const manifest=JSON.parse(await readFile(new URL("public/manifest.json",root),"utf8")),source=await readFile(new URL("public/icon.svg",root),"utf8"),pngs=await Promise.all([16,32,48,128].map(size=>readFile(new URL(`public/icon-${size}.png`,root))));assert.deepEqual(manifest.action.default_icon,{"16":"icon-16.png","32":"icon-32.png","48":"icon-48.png","128":"icon-128.png"});assert.deepEqual(manifest.icons,manifest.action.default_icon);assert.match(source,/#7047EB/);assert.match(source,/>PF<\/text>/);assert.match(source,/Arial Black/);assert.match(source,/font-size="68"/);assert.ok(pngs.every(file=>file.subarray(1,4).toString()==="PNG"))});
