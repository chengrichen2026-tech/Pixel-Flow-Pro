# Pixel Flow 团队生图｜伙伴安装指南

本指南适用于只使用“团队生图”的伙伴。你不需要 ChatGPT / Codex 账号，不需要 API Key，也不需要安装 Pixel Flow API Worker、Bridge 或 MCP。

## 1. 下载 Pixel Flow

打开：

<https://github.com/chengrichen2026-tech/Pixel-Flow-Pro>

选择 `Code → Download ZIP`，下载并解压。不要直接在压缩包内加载扩展。

## 2. 加载浏览器扩展

1. Chrome 打开 `chrome://extensions`；Edge 打开 `edge://extensions`。
2. 开启“开发者模式”。
3. 点击“加载已解压的扩展程序”。
4. 选择解压目录中的 `扩展程序` 文件夹。
5. 确认扩展显示 `Pixel Flow｜像素流`，版本为 `0.3.1` 或更新版本。

后续更新代码后，在扩展管理页点击一次“重新加载”。本地画布不会因源码更新被主动删除，但卸载扩展前仍建议先备份画布。

## 3. 导入团队生图凭证

主机管理员会单独发送一个本地 JSON 凭证文件。文件包含：

- `gatewayUrl`：临时或正式的团队网关地址；
- `memberName`：你的成员名称；
- `dailyLimit`：`null` 表示不限制每日次数；
- `token`：只属于你的 `pft_` 成员令牌。

不要把凭证文件上传到 GitHub、群文件或公开网盘。打开 Pixel Flow 左侧“生图设置”，在“团队生图”中分别填写 `gatewayUrl` 和 `token`，点击保存。

## 4. 运行第一张图

1. 新建生图任务。
2. 把模式切换为“团队生图”。
3. 输入一个简短提示词。
4. 点击“运行任务”。
5. 等待状态依次进入“排队中 / 生成中 / 已完成”。

验收成功必须同时看到：

- 任务状态为“已完成”；
- 上下文从 `0 轮` 变为 `1 轮`；
- 任务右侧出现“生成结果1”；
- 结果图片可以下载。

## 5. 常见问题

- `无法连接团队生图网关`：确认主机电脑在线，并向管理员索取当前有效的 `gatewayUrl`。Quick Tunnel 每次重启可能更换地址。
- `团队令牌无效或已撤销`：检查 token 是否完整，或请管理员重新创建成员令牌。
- 长时间排队：团队网关当前单并发执行，多位伙伴同时提交时会顺序排队。
- 生成失败或提示登录失效：由主机管理员刷新本机 Codex OAuth 后重试。

提示词和参考图会经团队网关转发到 OpenAI；主机管理员不会把自己的 OAuth 凭证发送到你的浏览器。
