# Pixel Flow 团队生图｜成员安装指南

本指南适用于使用 Team Cloud 或 GPT Web 团队模式的成员。你不需要 API Key，也不需要安装 Pixel Flow API Worker、Bridge 或 MCP。使用 GPT Web 团队模式时，提交电脑不需要登录 ChatGPT，也不需要配对成执行机；团队中只要有其他在线执行机即可。

## 1. 下载 Pixel Flow

打开：

<https://github.com/chengrichen2026-tech/Pixel-Flow-Pro>

选择 `Code → Download ZIP`，下载并解压。不要直接在压缩包内加载扩展。

## 2. 加载浏览器扩展

1. Chrome 打开 `chrome://extensions`；Edge 打开 `edge://extensions`。
2. 开启“开发者模式”。
3. 点击“加载已解压的扩展程序”。
4. 选择解压目录中的 `扩展程序` 文件夹。
5. 确认扩展显示 `Pixel Flow｜像素流`，版本为 `0.3.15` 或更新版本。

后续更新代码后，在扩展管理页点击一次“重新加载”。本地画布不会因源码更新被主动删除，但卸载扩展前仍建议先备份画布。

## 3. 保存成员令牌

管理员会单独发送成员令牌，格式以 `pfm_` 开头。它只用于成员身份、任务归属和 Team Cloud 每日额度。团队任务箱地址已内置，平台访问 Key 由安全中继托管，不再发送到成员浏览器。

不要把成员令牌上传到 GitHub、群文件或公开网盘。打开 Pixel Flow 顶部“生图设置”，在“团队成员”中填写成员令牌并保存。

成员作为任务发起方不需要配置“本机作为网页生图执行机”；该区域只用于真正承担 GPT Web 团队生图的 ChatGPT 执行机。

## 4. 运行第一张图

1. 新建生图任务。
2. 生图模式选择“Team Cloud”，或选择“GPT Web → 团队”。新建任务在已保存成员令牌时默认选择 GPT Web 团队。
3. 输入一个简短提示词。
4. 点击“运行任务”。
5. 等待任务按实际链路经过“排队 / 准备 / 发送 / 生成 / 写回”，最终进入“已完成”。

验收成功必须同时看到：

- 任务状态为“已完成”；
- 上下文从 `0 轮` 变为 `1 轮`；
- 任务右侧出现“生成结果1”；
- 结果图片可以下载。

## 5. 常见问题

- `无法连接团队生图服务`：确认网络正常，并检查固定团队中继是否可访问。
- `团队令牌无效或已撤销`：检查 token 是否完整，或请管理员重新创建成员令牌。
- `今日 Team Cloud 额度已用完`：等待北京时间次日00:00自动重置，或联系管理员调整每日额度。GPT Web 团队模式不消耗该额度。
- `当前没有在线执行机`：GPT Web 团队任务仍可提交排队；任意已配对执行机上线后会自动领取。
- `HTTP 429`：v0.3.4 起会按网关提示退避并从当前图片分块继续下载。已经生成完成且仍保留团队 Job 的任务不要直接重新生图，优先点击“恢复结果”。
- `Failed to fetch` 或长时间停在“图片已生成，正在回传结果”：更新到 v0.3.13；新版改由妙搭任务箱代理下载，不再要求伙伴浏览器直连 `workers.dev`。
- 长时间排队：Cloudflare 每5秒检测一次待处理任务，Queue 最多并发3；每分钟 Cron 负责检测器恢复和漏单兜底。持续超过2分钟仍排队时联系管理员检查 Queue 与任务箱状态。
- 生成失败或提示登录失效：由管理员重新封存 Codex OAuth 后重试，不需要伙伴登录 ChatGPT。

妙搭方案会临时保存任务提示词、参考图分块和结果分块，再由 Cloudflare Worker 调用 OpenAI；管理员不会把自己的 OAuth 凭证发送到妙搭或伙伴浏览器。Pixel Flow 下载完成后会签收任务，并在任务箱保留最近30条任务信息和结果预览。
