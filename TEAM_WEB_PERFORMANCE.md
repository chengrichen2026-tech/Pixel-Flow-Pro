# Team Web 性能诊断（2026-09-14）

状态：v0.3.12 已完成代码、本地验证、云端部署、GitHub 更新、执行机更新和真实双任务闭环验收。

## 线上证据

通过飞书 CLI 查询妙搭任务箱 online 环境中现存的 11 条 Team Web 任务时间记录，并读取 13:00–13:12 的执行机 Trace 和最新任务的交付 Trace；两个 Trace 查询均返回 has_more=false。没有发起生图、修改配置或读取图片正文。

下表为最近四单，时间均为秒。总计是 queued_at 到 completed_at，不含伙伴端最终下载和画布写入；“生成”是扩展记录的 submitted 到取得结果，包含网页等待、识别及图片获取，不是供应商纯推理时间。

| 任务 ID 前缀 | 排队 | 首次领取至提交 | 生成阶段 | 生成后至云端完成 | 总计 |
| --- | ---: | ---: | ---: | ---: | ---: |
| ac8a53d2 | 20.0 | 10.1 | 62.0 | 55.8 | 147.9 |
| 2fb07b0f | 577.6 | 8.8 | 64.6 | 37.9 | 688.9 |
| cfea3847 | 676.4 | 8.4 | 64.6 | 14.9 | 764.3 |
| 3f10279d | 74.3 | 697.7 | 57.0 | 23.9 | 853.0 |

### 执行机中断与队列等待

- 3f10279d 在 13:00:17 首次领取；旧设备最后心跳为 13:01:18。
- 此后查询范围内无 web-worker Trace，13:10:47 出现配对请求，13:10:48 再次领取，13:11:55 记录开始生成。
- 设备记录存在旧设备和新设备；旧设备仍标为 active，但 last_seen_at 停在 13:01:18。active 不代表持续在线。
- 服务端 claimJob 保留首次 startedAt，因此 697.7 秒不能全部解释为当前执行机上传/发送耗时。
- 证据支持执行链路曾中断并在重新配对后恢复；无法仅凭云端记录区分关机、休眠、网络中断、扩展暂停或重载。
- 单机仅持有一个 activeTeamWebJob，且有本机 browser 任务时不领取团队任务，放大后续排队。不能把这批十多分钟等待全部归因于 ChatGPT 模型速度。

### 重复回传与缺失互斥

- 最新 ac8a53d2 任务交付 Trace：generated 2 次、result-chunks POST 10 次、preview-chunks 2 次、complete 2 次；结果下载索引为 0–4，每个索引 GET 2 次。
- 该模式强烈支持重复收集/交付；Trace 不含上传块索引，不能据此证明每一次 POST 对应的具体块。
- contentScript 的 RESUME_CHATGPT_RESULT 分支仅检查 activeResumeTasks，没有用 activeSubmitTasks 阻止与原 submitTask 的结果等待并行。
- background 的恢复循环读取 submitActive 后没有用它阻止恢复监听；Team Web 的 completeActiveTeamWebJob 没有任务级交付互斥，两个 TASK_RESULT 可同时进入上传。
- 另外两单在完成附近出现 result-chunks 409“任务未由当前 Worker 持有”，与完成后仍存在另一条回传路径相符；不是已证实的两台设备同时生成。
- 普通团队结果轮询与恢复轮询也可能重复下载；最新任务确有两套分块 GET，具体来源仍需运行时事务标识确认。

### 分块传输与轮询

- Team Web 被服务端指定为 chunks，未复用 Team Cloud 的 direct。
- 分块大小为 600,000 Base64 字符；同图内逐块上传和下载。执行机上传经过 Cloudflare 到妙搭，伙伴端下载每块之间另等 250ms。
- 最新单 generated_at 到 completed_at 为 55.8 秒；包括请求往返、重复交付、预览和完成确认，不能把全部时间等同于纯网络上传。
- 执行机空闲接单约每 30 秒；正常完成后约 1 秒主动再检查。伙伴端结果轮询间隔为 2 秒。
- 最新单完成后仍尝试上传成员预览，服务端返回 409“任务当前不接受直传预览图”；该请求发生在结果写回之后，不是主要等待原因。

## 修复优先级与验收

1. 先修交付互斥：同一任务重复 TASK_RESULT 共用一次交付；完成/失败必须核对任务身份，避免迟到事件清掉下一单。回归覆盖原监听与恢复监听同时结束、重复消息、上传失败、迟到事件和下一单隔离。
2. 补执行机在线状态及中断提示：区分排队、等待执行机、等待手动操作和实际生成；不在缺乏提交证据时自动重发，避免重复生成。
3. 再实施整图直传：保留旧 chunks 任务兼容，覆盖多图、校验、令牌归属、过期和重复提交。现有 direct 完成协议只接受一张，不能只放开 provider 条件就宣布支持 Team Web 多图。
4. 验收使用独立临时画布，确认同任务只回传一次、结果写回、无重复 409、下一单可继续；比较相同输入的阶段时间。测试/构建通过不代表真实产品已验收。

## 验证边界

- 本轮未更改产品代码、部署、配对状态或现有任务，未运行产品行为测试。
- 本地 public/background.js 与扩展程序/background.js 内容一致；未凭此认定远端执行机加载了相同版本。
- 结论以本次云端时间与 Trace 为证据；分块直传的可节省时长尚未实测。


## 2026-09-14 修复实现与发布门槛

- 扩展 v0.3.12 / ChatGPT adapter 26：原提交监听与恢复监听互斥；回传和下载共享任务级 Promise；失败、清理和迟到消息核对任务对象；接单 tick 互斥。
- 协议 v6：新 Team Web 任务选择 bundle。执行机先上传轻量预览，再经中继一次上传 JSON 多图结果包；私有 KV 暂存 1 小时，短时签名地址供伙伴一次下载，并检查大小、SHA-256、结果包格式。超过 18,000,000 字节自动切回 chunks；Cloud 旧 direct 协议保持单图语义。
- 中继校验当前设备令牌与任务归属；私有回执绑定令牌摘要，完成请求重试复用已暂存结果；旧设备未声明 supportsBundle 时只领取旧格式。
- 服务端基于实际 lastSeenAt 判断断线，保留手动发送提示；提交时记录 generationStartedAt；任务箱与扩展任务卡展示进度详情。
- 验证：扩展 141 项全量测试；任务箱 20 项测试与中继 13 项测试，均通过。新增行为测试覆盖重复回传、上传失败、迟到错误、大小/SHA 校验、多图打包、超大包回退、令牌撤销、归属错误、重复完成、离线状态。两项目类型检查与生产构建通过。
- 本地 UI：实际 TaskBoard 组件在隔离页面显示等待上线、连接中断、回传中，DOM 验证 3 条提示均无水平溢出；此证据是本地组件验证，不是线上页面验收。未触碰真实任务。
- 发布顺序：部署 Cloudflare 中继 → 推送并发布妙搭后端（确认 health 协议 6）→ 更新执行机及伙伴扩展 v0.3.12 → 独立临时画布验证单图、多图及下一单，回读云端终态、画布资产和任务日志 → 清理临时数据并回读。
- 线上验收需要云端发布授权及可用执行机；目前没有进行远端推送、部署或真实生图，不能报告速度改善已实测。


## 2026-09-14 云端部署回读

- 用户已确认云端部署、扩展更新及真实验收。
- Cloudflare 版本 `097db2e2-c867-4ab3-b21d-ace2c1910957` 已部署，health 返回 ok。
- 妙搭发布 `7685266148233055515` 回读 finished，发布 commit 为 `5f34954b24f1a71fe7c4d6f6fad2ca36e107023a`；业务 health 返回协议 v6。
- 实际任务箱页面刷新后显示服务正常、1 台执行机在线；云端 11 条既有 Team Web 任务保持 completed。
- Chrome 连接器能读取正常页面，但不能操作 chrome://extensions；Computer Use 两次读取 Chrome 均超时。已请用户手动重新加载现有 Pixel Flow 扩展；未重装、未修改权限，未启动新验收任务。
- 本轮完成云端上线，尚不能报告执行机已更新或速度已实测改善。

## 2026-09-14 GitHub 更新回读

- 公开仓库固定为 `https://github.com/chengrichen2026-tech/Pixel-Flow-Pro.git`，分支 `main`。
- v0.3.12 提交 `0c7728c912eaf769e5e954297b07ddfd8d587cfb` 已推送；fetch 后本地与 `origin/main` SHA 完全一致。
- 公开快照独立通过 141 项测试、TypeScript 检查和生产构建；远端 `package.json` 与扩展 manifest 均回读为 0.3.12，新版 bundle 接单与去重代码存在。
- 发布前暂存内容和发布后远端树均未命中 runtime、imports、artifacts、QA、画布文件、日志或内部诊断文档；未命中实际成员令牌、设备令牌或私有妙搭应用地址。
- 当前真实验收任务 `5e7e810a-3549-4c99-a098-433fe4e930f0` 已创建为 bundle 并保持 queued；旧执行机持续发起 claim 但不会误领新格式。等待另一台电脑更新并重载 v0.3.12，不重复提交。

## 2026-09-14 真实双任务验收

- 另一台网页生图机更新至 GitHub `0c7728c` 并重载后，原排队任务在下一轮询周期自动领取；没有重复运行任务。
- 第一单：执行机领取后准备 14.3 秒、生成 42.8 秒、交付 11.9 秒，领取到云端完成共 69.0 秒。伙伴画布回读为 completed、runCount=1、1 个结果节点、1 个结果资产和 1 条输出边。
- 第二单：入队 9.9 秒后被同一执行机领取，准备 8.0 秒、生成 143.9 秒、交付 29.6 秒；伙伴画布同样回读 completed、runCount=1、1 个结果节点、1 个结果资产和 1 条输出边。
- 两单 Trace 均为 generated=1、complete=1、旧 result-chunks=0；数据库签收后均只保留 1 个 preview 分块，完整结果已按既有规则清理。证明重复回传已消除，bundle 路径真实生效。
- 相比修复前最近一单 55.8 秒交付，两个新任务分别缩短至 11.9 秒和 29.6 秒，约减少 79% 和 47%。生成阶段仍受 ChatGPT 本身波动影响，第二单的 143.9 秒属于模型生成时间，不是接单或分块传输。
- 第二单在第一单结束后正常接续，证明执行机释放和下一单领取闭环通过。
- 临时验收画布 `project-e430a648-9243-49f3-83cc-8ad1c3299ab2` 已通过结构化命令删除；最终项目列表回读不存在，并已切回原空画布。云端保留两条轻量 completed 历史用于任务箱记录。

## 2026-09-14 当前源码性能收敛

- 对照旧版确认：旧版快并非采用另一套结果协议；快路径同样是 ChatGPT DOM 识别与 bundle 回传。主要差异来自运行时是否干净、是否重复监听，以及结果识别和上传阶段的额外等待。
- 当前源码把原图 bundle 上传和 complete 放到预览生成之前；480px 预览改为完成后的 best-effort 操作，不再阻塞成员画布写回。
- ChatGPT 对话 URL 在 `/c/...` 确立时立即持久化；标签页就绪新增关闭检测和 30 秒上限；扩展重载会恢复未建立具体对话的活动 Team Web Job，并清理仅属于 Pixel Flow 且没有活跃映射的孤儿任务标签。
- content script adapter 28 将结果识别、参考图上传与输入区就绪等待改为 MutationObserver 唤醒，保留 200/250/500ms 兜底，不再完全依赖隐藏标签页中易被节流的定时轮询。
- adapter 27 真实验收任务 `709f19c7…`：排队 19.5 秒、生成阶段 62.4 秒、交付 10.1 秒、总计 163.2 秒；结果已真实写回，TaskNode 与 TaskRun 均为 completed。交付速度已回到旧版 11.9 秒快路径区间。
- adapter 28 复验时任务箱显示服务正常但生图机在线为 0，Job `dcfe5f8a…` 保持 queued；本地旧逻辑因一次网关连接失败提前标记 failed。现已改为已有 Job 遇网络瞬断时保持轮询并显示自动重连，不创建重复 Job。
- 删除任务现在优先使用 TaskNode 的 `apiJobId`，缺失时回退 TaskRun 的 `providerJobId` 清理远端 Job。任务箱协议 v8 新增幂等 `POST /openapi/jobs/:id/cancel`；旧 DELETE 继续只处理终态签收。
- 本轮 176 项测试、TypeScript、生产构建和真实扩展重载通过。adapter 28 因执行机离线未完成新的端到端生图，不能把 adapter 27 的实测结果冒充为 adapter 28 闭环。

## 2026-09-14 取消协议 v8 验收

- 任务箱将 canceled 作为受控终态：成员只能取消自己的 uploading、queued 或 running Job；重复取消保持 canceled；取消后清除 Worker 租约、结果元数据和全部图片分块，迟到的心跳、上传与完成请求因任务不再 running 而被拒绝。
- 妙搭 release `7685372974262586546` 已 finished，线上 commit 为 `4790397d4606937c0e08ea9b0625869691db8b5d`；health 回读 protocolVersion 8，取消接口回读 HTTP 200。
- 遗留 adapter 28 Job `dcfe5f8a…` 已取消。另创建一条不含图片的排队验收任务：Pixel Flow TaskRun 从 generating 进入 canceled，任务节点删除；云端同一 Job `1da03313…` 回读 canceled；任务箱进行中回到 0，没有打开 ChatGPT 或触发生图。
- 验收临时 TaskRun 已通过结构化命令删除，画布不存在临时任务；成员现有 Browser 任务未被取消链路影响。
